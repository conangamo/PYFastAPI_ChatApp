# Flet Desktop App Dependencies
# Core Framework
flet==0.23.2

# HTTP & WebSocket Client
httpx==0.25.1
websockets==12.0

# Data Handling
pydantic==2.5.0
python-dateutil==2.8.2

# Utils
python-dotenv==1.0.0
aiofiles==23.2.1

# Image Processing
Pillow==10.1.0

# Audio Processing (Voice Messages)
sounddevice==0.4.6      # Record audio from microphone
soundfile==0.12.1       # Save/load audio files
numpy>=2.0,<2.3.0       # Audio data array processing (compatible with opencv-python)
platformdirs==4.1.0     # Cross-platform app directories

# Video Call & WebRTC
aiortc==1.6.0           # WebRTC implementation for Python
opencv-python==4.12.0.88 # Computer vision library for video processing
# Note: audioop-lts not needed for Python 3.12 (audioop is still available)

